'use client';

import { MetaConnectionCard } from '@/components/meta-connection-card';

/** Facebook Page — хуудасны чатыг WinFit дотроос харж хариулах. */
export default function FacebookConnectionSettings() {
  return <MetaConnectionCard />;
}
